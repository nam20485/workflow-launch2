### **Boomerang Prompt:**

"Hello Dotnet Agents. I'm ready to continue our implementation.

I have your persona instructions (`DotnetAgentsPersona.md`) and our complete implementation plan (`LoneAgentGuide.md`) in context.

We just finished creating the `AgentTask` model and the `AgentDbContext`. Based on Chapter 4 of our guide, I'm ready to build the next part.

Can you show me how to:

1. Register the `AgentDbContext` in my `AgentApi/Program.cs`?  
2. Create the API endpoints (`POST /api/tasks` and `GET /api/tasks/{id}`)?  
3. Implement the full `AgentWorkerService` class?"

